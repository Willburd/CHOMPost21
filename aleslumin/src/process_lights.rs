use meowtonin::{ByondError, ByondResult, value::ByondValue};

#[byond_fn]
pub fn generate_automata(
    _input: Vec<ByondValue>,
) -> ByondResult<Vec<ByondValue>> {

    let byond_list: Vec<ByondValue> = Vec::new();
    Ok(byond_list)
}
